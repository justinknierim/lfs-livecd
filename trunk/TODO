* Re-work the download system and the way package variables are stored ***DONE***
* Check that chroot system mounts are being done correctly (proc, sys, etc.) ***DONE**
* Finish up the chroot build section for the lfs-base ***DONE***
* Extend the base system to include BLFS packages ***IN PROGRESS***
	Some packages need blfs bootscripts [ openssh, gpm ]

* WWW_HOME variable in /etc/profile should be set for lynx
* Check for existence of $(WD)/sources before making the dir ***DONE***
* Add a note to the Makefile about requiring an internet connection
* Add a section for building a cdrtools and squashfs (outside the cd system)
* Add custom bootscript package to svn repo
* Setup kernel build -- include initramfs build
* squash up /usr
* Build the iso
